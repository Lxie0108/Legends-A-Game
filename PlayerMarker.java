/**
 * 
 * A type of marker that represents where player is at. It is represented by "X".
 *
 */
public class PlayerMarker extends Marker {
	final static String heroMarker = "X";
	public PlayerMarker() {
		this.symbol = heroMarker;
	}
}
